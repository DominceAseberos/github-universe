# Configuration and constants for backend
from pathlib import Path

RAW_DIR = Path("../data/raw")
OUT_DIR = Path("../data")
COUNTRY_META = {
    # Example: 'US': {'name': 'United States', 'flag': '🇺🇸', 'continent': 'NA'}
    # Fill with real metadata as needed
}
